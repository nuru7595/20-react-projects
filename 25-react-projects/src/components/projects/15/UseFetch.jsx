export default function UseFetch({ title }) {
    return (
        <section>
            <h2 className="title">{title}</h2>
            <h3 className="text-center py-6">Up Next...</h3>
        </section>
    );
}
