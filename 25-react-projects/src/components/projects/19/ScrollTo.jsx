export default function ScrollTo({ title }) {
    return (
        <section>
            <h2 className="title">{title}</h2>
            <div className="px-3 py-6 text-center font-bold">
                <h3>Working...</h3>
            </div>
        </section>
    );
}
